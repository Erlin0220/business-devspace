name: Native installer candidates

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: native-candidates-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build:
    if: github.ref == 'refs/heads/main'
    environment: validation
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: windows-2022
            target: win32-x64
          - runner: ubuntu-24.04
            target: linux-x64
          - runner: macos-15
            target: darwin-arm64
          - runner: macos-15-intel
            target: darwin-x64
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 60
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - id: metadata
        run: node scripts/ci-metadata.mjs
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: ${{ steps.metadata.outputs.node }}
          package-manager-cache: false
      - name: Require matching native architecture and sample-only configuration
        env:
          EXPECTED_TARGET: ${{ matrix.target }}
        run: |
          node --input-type=module -e "import release from './scripts/release-profile.mjs'; if(process.platform+'-'+process.arch!==process.env.EXPECTED_TARGET)throw Error('Native runner mismatch'); if(release.gateway!=='https://gateway.example.com'||release.distribution.origin!=='https://downloads.example.com')throw Error('Public validation must not embed operator configuration');"
      - name: Install locked build tools
        env:
          BUILD_NPM: ${{ steps.metadata.outputs.npm }}
        run: npx --yes "$BUILD_NPM" ci --no-audit --no-fund
      - name: Verify native Windows bootstrap regressions
        if: runner.os == 'Windows'
        run: node --test test/windows-bootstrap.test.mjs
      - id: go-version
        if: runner.os == 'macOS'
        run: node -e "const fs=require('fs'),r=require('./release.config.json');fs.appendFileSync(process.env.GITHUB_OUTPUT,'version='+r.cloudflaredGoVersion+'\n')"
      - uses: actions/setup-go@924ae3a1cded613372ab5595356fb5720e22ba16 # v6
        if: runner.os == 'macOS'
        with:
          go-version: ${{ steps.go-version.outputs.version }}
          cache: false
      - name: Build pinned cloudflared on matching native macOS
        if: runner.os == 'macOS'
        run: node scripts/macos-cloudflared.mjs
      - name: Build installer from this exact checkout
        run: npm run package
      - name: Verify final package layout
        run: node scripts/verify-release.mjs --target "$EXPECTED_TARGET"
        env:
          EXPECTED_TARGET: ${{ matrix.target }}
      - name: Audit exact redistributed bytes and retained licenses
        env:
          EXPECTED_TARGET: ${{ matrix.target }}
        run: |
          mkdir -p "build/redistribution/$EXPECTED_TARGET"
          node scripts/audit-inventory.mjs \
            --directory "build/bundle-$EXPECTED_TARGET" \
            --output "build/redistribution/$EXPECTED_TARGET/syft.json"
          node scripts/verify-redistribution.mjs \
            --bundle "build/bundle-$EXPECTED_TARGET" \
            --inventory "build/redistribution/$EXPECTED_TARGET/syft.json.summary.json" \
            --output "build/redistribution/$EXPECTED_TARGET/evidence.json"
      - name: Accept final Windows installer in the disposable runner
        if: runner.os == 'Windows'
        env:
          TEAM_DEVSPACE_UPGRADE_BASELINE_ORIGIN: ${{ secrets.UPGRADE_BASELINE_ORIGIN }}
        run: npm run acceptance:platform -- --direct-windows-installer
      - name: Accept final macOS package in the disposable runner
        if: runner.os == 'macOS'
        env:
          TEAM_DEVSPACE_UPGRADE_BASELINE_ORIGIN: ${{ secrets.UPGRADE_BASELINE_ORIGIN }}
        run: npm run acceptance:platform -- --system-macos-installer
      - name: Accept final Linux archive with the user service manager
        if: runner.os == 'Linux'
        env:
          TEAM_DEVSPACE_UPGRADE_BASELINE_ORIGIN: ${{ secrets.UPGRADE_BASELINE_ORIGIN }}
        run: |
          export XDG_RUNTIME_DIR="/run/user/$(id -u)"
          export DBUS_SESSION_BUS_ADDRESS="unix:path=$XDG_RUNTIME_DIR/bus"
          sudo loginctl enable-linger "$(id -un)"
          sudo systemctl start "user@$(id -u).service"
          systemctl --user show-environment >/dev/null
          npm run acceptance:platform
      - name: Retain acceptance and redistribution evidence only - installer bytes remain blocked
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: evidence-${{ matrix.target }}-${{ github.sha }}
          path: |
            release/offline/${{ steps.metadata.outputs.version }}/${{ matrix.target }}/acceptance.json
            build/redistribution/${{ matrix.target }}/syft.json.summary.json
            build/redistribution/${{ matrix.target }}/evidence.json
            build/bundle-${{ matrix.target }}/sbom.cdx.json
            build/bundle-${{ matrix.target }}/THIRD-PARTY-NOTICES.txt
            build/bundle-${{ matrix.target }}/release-provenance.json
            build/bundle-${{ matrix.target }}/LICENSES/**
          if-no-files-found: error
          retention-days: 30

# Source-only public CI: no operator profile, production signing key, employee
# credentials, installer uploads, Releases, deployment or rollout mutation.
# Old immutable installers are read-only test inputs, never public artifacts.
# Candidate artifacts contain review metadata/license text only. Installer bytes
# stay local to each disposable runner until the separate public-binary gate is closed.
